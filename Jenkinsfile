pipeline {
    agent any

    stages {
    	stage('Aula Jenkins 1') {
				steps {
					sh '''
						
						docker info
						java --version
						docker compose version
						
					'''
				}
    	}
    }
}
